/**
 * Legacy Orb shell placeholder.
 *
 * The historical implementation referenced internal packages that no longer exist.
 * Keep this stub until the new shell is re-scoped.
 */

export async function runOrbShellDemo(): Promise<void> {
  console.warn('[orb-web] runOrbShellDemo is deprecated – use the web console instead.');
}

