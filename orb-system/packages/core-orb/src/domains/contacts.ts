/**
 * Contacts Domain
 *
 * Re-exports the core contact graph types while anchoring them inside
 * the `domains/` namespace requested in the SomaOS porting plan.
 */

export * from '../contacts/types';

